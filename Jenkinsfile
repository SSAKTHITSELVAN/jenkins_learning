pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Building App'
            }
        }

        stage('Test') {
            steps {
                sh 'echo Tests Passed'
            }
        }
    }
}